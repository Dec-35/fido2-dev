import pool from './../config.js';

function createUser(userDetails) {
  // create a new user
}

function getUserByEmail(email) {
  // get user by email
}

function addPublicKeyToUser(email, publicKey) {
  // add public FIDO key to user
}

export {createUser, getUserByEmail, addPublicKeyToUser};
